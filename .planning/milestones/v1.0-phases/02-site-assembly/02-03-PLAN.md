---
phase: 02-site-assembly
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - docusaurus.config.ts
  - specs/authorization.json
  - specs/sim-management.json
  - specs/order-management.json
  - specs/product-information.json
  - specs/support-management.json
  - specs/1nce-os.json
  - docs/api/
  - sidebars/api.ts
  - package.json
autonomous: true
requirements: [API-01, API-02, API-03, API-04]

must_haves:
  truths:
    - "All 6 OpenAPI specs render interactive Try It panels where a developer can execute API calls"
    - "API Explorer tab shows content organized by spec: Authorization, SIM Management, Order Management, Product Information, Support Management, 1NCE OS"
    - "Auto-generated code snippets appear in multiple languages for each endpoint"
    - "API Explorer navbar tab navigates to the API docs section with a working sidebar"
  artifacts:
    - path: "specs/authorization.json"
      provides: "Authorization API spec"
    - path: "specs/sim-management.json"
      provides: "SIM Management API spec"
    - path: "specs/order-management.json"
      provides: "Order Management API spec"
    - path: "specs/product-information.json"
      provides: "Product Information API spec"
    - path: "specs/support-management.json"
      provides: "Support Management API spec"
    - path: "specs/1nce-os.json"
      provides: "1NCE OS API spec"
    - path: "docs/api/"
      provides: "Generated API documentation MDX pages"
    - path: "docusaurus.config.ts"
      provides: "OpenAPI plugin + theme + API docs instance config"
      contains: "docusaurus-plugin-openapi-docs"
  key_links:
    - from: "docusaurus.config.ts openapi plugin"
      to: "specs/*.json"
      via: "specPath config"
      pattern: "specPath.*specs/"
    - from: "docusaurus.config.ts openapi plugin"
      to: "docs instance id: 'api'"
      via: "docsPluginId"
      pattern: "docsPluginId.*api"
    - from: "docusaurus.config.ts api docs instance"
      to: "@theme/ApiItem"
      via: "docItemComponent"
      pattern: "docItemComponent.*ApiItem"
---

<objective>
Install the OpenAPI docs plugin/theme, configure all 6 API specs, generate API documentation pages with interactive Try It panels, and wire the API Explorer navbar tab to the API docs instance.

Purpose: The API Explorer is a core feature -- developers need interactive Try It panels to test API endpoints, matching the current ReadMe.com experience.
Output: Fully functional API Explorer tab with 6 spec categories, interactive Try It panels, and auto-generated code snippets.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-site-assembly/02-CONTEXT.md
@.planning/phases/02-site-assembly/02-RESEARCH.md
@.planning/phases/02-site-assembly/02-01-SUMMARY.md

<interfaces>
<!-- Plan 01 creates multi-instance docs config. This plan adds the API docs instance. -->
<!-- Plan 01 leaves API Explorer as a simple `to: '/api'` navbar link. This plan converts it to docSidebar type. -->

Expected state after Plan 01:
- docusaurus.config.ts has plugins array with platform, blueprints, terms instances
- navbar has 5 items, API Explorer is `{ to: '/api', label: 'API Explorer' }`
- docs/reference/ still exists with ReadMe-exported API markdown pages

OpenAPI spec files available at:
- dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/authorization.json
- dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/sim-management.json
- dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/order-management.json
- dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/product-information.json
- dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/support-management.json
- dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/1nce-os.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install OpenAPI plugin/theme, copy specs, and configure API docs instance</name>
  <files>package.json, specs/, docusaurus.config.ts, sidebars/api.ts</files>
  <read_first>
    - /Users/Jan.Sulaiman/_Projects/docusaurus_poc/docusaurus.config.ts
    - /Users/Jan.Sulaiman/_Projects/docusaurus_poc/package.json
    - /Users/Jan.Sulaiman/_Projects/docusaurus_poc/.planning/phases/02-site-assembly/02-RESEARCH.md
    - /Users/Jan.Sulaiman/_Projects/docusaurus_poc/dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/authorization.json (first 20 lines, to confirm structure)
  </read_first>
  <action>
**A. Install dependencies** (per RESEARCH.md):
```bash
npm install docusaurus-plugin-openapi-docs@4.7.1 docusaurus-theme-openapi-docs@4.7.1 docusaurus-plugin-sass@0.2.6 sass @docusaurus/plugin-content-docs@3.9.2
```

NOTE: `@docusaurus/plugin-content-docs@3.9.2` may already be installed by Plan 01. If so, the install is a no-op for that package.

CRITICAL: `docusaurus-plugin-sass` and `sass` are required peer dependencies of `docusaurus-theme-openapi-docs` (per RESEARCH.md Pitfall 3). Without them the build crashes.

**B. Copy OpenAPI spec files** to `specs/` directory at project root:
```bash
mkdir -p specs
cp dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/authorization.json specs/
cp dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/sim-management.json specs/
cp dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/order-management.json specs/
cp dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/product-information.json specs/
cp dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/support-management.json specs/
cp dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/1nce-os.json specs/
```

**C. Add API docs instance** to `docusaurus.config.ts` plugins array (alongside existing platform, blueprints, terms instances from Plan 01):

```typescript
['@docusaurus/plugin-content-docs', {
  id: 'api',
  path: 'docs/api',
  routeBasePath: '/api',
  sidebarPath: './sidebars/api.ts',
  docItemComponent: '@theme/ApiItem', // REQUIRED for Try It panels (per RESEARCH.md Pitfall 2)
}],
```

**D. Add OpenAPI plugin** to the plugins array (per D-06, D-08, D-10):

```typescript
['docusaurus-plugin-openapi-docs', {
  id: 'openapi',
  docsPluginId: 'api',
  config: {
    authorization: {
      specPath: 'specs/authorization.json',
      outputDir: 'docs/api/authorization',
      sidebarOptions: { groupPathsBy: 'tag', categoryLinkSource: 'tag' },
    },
    'sim-management': {
      specPath: 'specs/sim-management.json',
      outputDir: 'docs/api/sim-management',
      sidebarOptions: { groupPathsBy: 'tag', categoryLinkSource: 'tag' },
    },
    'order-management': {
      specPath: 'specs/order-management.json',
      outputDir: 'docs/api/order-management',
      sidebarOptions: { groupPathsBy: 'tag', categoryLinkSource: 'tag' },
    },
    'product-information': {
      specPath: 'specs/product-information.json',
      outputDir: 'docs/api/product-information',
      sidebarOptions: { groupPathsBy: 'tag', categoryLinkSource: 'tag' },
    },
    'support-management': {
      specPath: 'specs/support-management.json',
      outputDir: 'docs/api/support-management',
      sidebarOptions: { groupPathsBy: 'tag', categoryLinkSource: 'tag' },
    },
    '1nce-os': {
      specPath: 'specs/1nce-os.json',
      outputDir: 'docs/api/1nce-os',
      sidebarOptions: { groupPathsBy: 'tag', categoryLinkSource: 'tag' },
    },
  },
}],
```

**E. Add theme** to config:
```typescript
themes: ['docusaurus-theme-openapi-docs'],
```

**F. Add `docusaurus-plugin-sass`** to the plugins array:
```typescript
'docusaurus-plugin-sass',
```

**G. Create `sidebars/api.ts`** for the API docs instance. This will be auto-populated by the OpenAPI plugin's generated sidebar, but needs an initial file:

```typescript
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';
const sidebars: SidebarsConfig = {
  apiSidebar: [{type: 'autogenerated', dirName: '.'}],
};
export default sidebars;
```

**H. Update the API Explorer navbar item** from the simple link placeholder (set in Plan 01) to a proper docSidebar item:

```typescript
{ type: 'docSidebar', sidebarId: 'apiSidebar', label: 'API Explorer', position: 'left', docsPluginId: 'api' },
```

**I. Handle the API Introduction landing page** (per D-07): Copy the API introduction markdown from the ReadMe export to serve as the API Explorer landing page. The file at `docs/reference/api-introduction/api-welcome.md` (or similar) should be copied to `docs/api/index.md` with appropriate frontmatter (`slug: /`) so it becomes the default page when clicking the API Explorer tab. Also consider copying `api-best-practices.md`, `api-rate-limits.md`, and `api-authorization.md` into `docs/api/` as introductory pages alongside the generated spec pages.

**J. Clean up old reference docs** from `docs/reference/` that are now replaced by the OpenAPI-generated pages. The hand-written API endpoint docs in `docs/reference/authorization/`, `docs/reference/sim-management/`, etc. are replaced by the plugin-generated MDX. However, keep `docs/reference/api-introduction/` and `docs/reference/readmeconfig/` content -- move useful parts to `docs/api/` or `docs/documentation/` as appropriate. The `docs/reference/mcp-server/` content should go to `docs/documentation/` (it's already there from Phase 1 as `docs/documentation/mcp-server/`).

Remove or move `docs/reference/` after extracting useful content so it does not conflict with the multi-instance setup.
  </action>
  <verify>
    <automated>cd /Users/Jan.Sulaiman/_Projects/docusaurus_poc && test -f specs/authorization.json && test -f specs/sim-management.json && test -f specs/1nce-os.json && grep "docusaurus-plugin-openapi-docs" package.json && grep "docusaurus-theme-openapi-docs" package.json && grep "docusaurus-plugin-sass" package.json && grep "docsPluginId.*api" docusaurus.config.ts && echo "CONFIG CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - specs/authorization.json exists (copied from export)
    - specs/sim-management.json exists
    - specs/order-management.json exists
    - specs/product-information.json exists
    - specs/support-management.json exists
    - specs/1nce-os.json exists
    - package.json contains `docusaurus-plugin-openapi-docs` in dependencies
    - package.json contains `docusaurus-theme-openapi-docs` in dependencies
    - package.json contains `docusaurus-plugin-sass` in dependencies
    - package.json contains `sass` in dependencies
    - docusaurus.config.ts contains `id: 'api'` plugin instance
    - docusaurus.config.ts contains `docItemComponent: '@theme/ApiItem'`
    - docusaurus.config.ts contains `docsPluginId: 'api'` in openapi plugin
    - docusaurus.config.ts contains `specPath: 'specs/authorization.json'`
    - docusaurus.config.ts contains `themes: ['docusaurus-theme-openapi-docs']`
    - docusaurus.config.ts contains `docusaurus-plugin-sass` in plugins
    - sidebars/api.ts exists with apiSidebar export
    - docusaurus.config.ts navbar contains `docsPluginId: 'api'` for API Explorer item
  </acceptance_criteria>
  <done>OpenAPI plugin and theme installed. All 6 specs copied to specs/. API docs instance configured with @theme/ApiItem for Try It panels. Navbar API Explorer item wired to API docs instance.</done>
</task>

<task type="auto">
  <name>Task 2: Generate API docs from specs and verify build with Try It panels</name>
  <files>docs/api/ (generated MDX pages)</files>
  <read_first>
    - /Users/Jan.Sulaiman/_Projects/docusaurus_poc/docusaurus.config.ts
    - /Users/Jan.Sulaiman/_Projects/docusaurus_poc/sidebars/api.ts
    - /Users/Jan.Sulaiman/_Projects/docusaurus_poc/specs/authorization.json (first 30 lines)
  </read_first>
  <action>
**A. Generate API documentation** from all 6 specs:

```bash
npx docusaurus gen-api-docs all
```

This reads each spec from `specs/*.json` and generates MDX pages into `docs/api/{spec-name}/` directories. Each generated page includes the interactive Try It panel markup via the `@theme/ApiItem` component.

**B. Verify the generated output:**
- `docs/api/authorization/` should contain generated MDX files for Authorization endpoints
- `docs/api/sim-management/` should contain generated MDX files for SIM Management endpoints
- `docs/api/order-management/` should contain generated MDX files for Order Management endpoints
- `docs/api/product-information/` should contain generated MDX files for Product Information endpoints
- `docs/api/support-management/` should contain generated MDX files for Support Management endpoints
- `docs/api/1nce-os/` should contain generated MDX files for 1NCE OS endpoints

**C. Check the generated sidebar** -- the OpenAPI plugin may generate its own sidebar file or sidebar slice. If the plugin generates a `sidebar.ts` or modifies `sidebars/api.ts`, verify it includes all 6 spec categories. If not, the `autogenerated` sidebar in `sidebars/api.ts` should pick up the generated directories automatically.

**D. Run the full build:**
```bash
npx docusaurus build
```

The build must complete with zero errors. Check that:
- No broken links to API pages
- All 6 spec categories appear in the API sidebar
- The API Explorer tab in the navbar works

**E. If the build fails** due to:
- Missing `docs/api/index.md`: Create one with frontmatter `slug: /` and a brief API Explorer introduction
- Sidebar conflicts: Adjust `sidebars/api.ts` to use the plugin-generated sidebar export if available
- Path conflicts with old `docs/reference/` content: Remove `docs/reference/` if it causes routing conflicts
- SASS compilation errors: Verify `docusaurus-plugin-sass` is in the plugins array

**F. Commit generated API docs** (per RESEARCH.md Pitfall 4 -- commit generated files so CI builds work without running gen-api-docs).
  </action>
  <verify>
    <automated>cd /Users/Jan.Sulaiman/_Projects/docusaurus_poc && ls docs/api/authorization/ | head -5 && ls docs/api/sim-management/ | head -5 && ls docs/api/1nce-os/ | head -5 && npx docusaurus build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - docs/api/authorization/ contains generated MDX files
    - docs/api/sim-management/ contains generated MDX files
    - docs/api/order-management/ contains generated MDX files
    - docs/api/product-information/ contains generated MDX files
    - docs/api/support-management/ contains generated MDX files
    - docs/api/1nce-os/ contains generated MDX files
    - `npx docusaurus build` exits with code 0
    - Build output mentions no broken links for /api/ routes
    - docs/api/ contains at least 30 generated MDX files total (6 specs with multiple endpoints each)
  </acceptance_criteria>
  <done>All 6 OpenAPI specs generate interactive API documentation. The build passes. API Explorer tab shows all 6 spec categories with Try It panels on each endpoint page.</done>
</task>

</tasks>

<verification>
- `npx docusaurus build` completes with zero errors
- API Explorer tab navigates to API docs section
- All 6 spec categories visible in API sidebar
- Generated API pages contain Try It panel markup (check for `@theme/ApiItem` or `api-item` in output)
- Code snippets appear for endpoints
</verification>

<success_criteria>
- 6 OpenAPI specs integrated via docusaurus-plugin-openapi-docs
- Interactive Try It panels render for all API endpoints
- Auto-generated code snippets in multiple languages
- API docs organized by spec (Authorization, SIM Management, Order Management, Product Information, Support Management, 1NCE OS)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-site-assembly/02-03-SUMMARY.md`
</output>
