---
phase: 08-branding-visual-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docusaurus.config.ts
  - static/img/favicon.ico
  - static/img/docusaurus-social-card.jpg
  - static/img/1nce-social-card.png
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - NAV-01

must_haves:
  truths:
    - "Browser tab shows the 1NCE favicon, not the Docusaurus dinosaur"
    - "Navbar displays the 1NCE SVG logo with '1NCE Developer Hub' title"
    - "No dark mode toggle is visible in the navbar"
    - "OS-level dark mode preference is ignored — site renders light only"
    - "Navbar contains 1NCE Home, 1NCE Shop, and 1NCE Portal links opening in new tabs"
    - "Social sharing previews show 1NCE-branded image instead of Docusaurus default"
  artifacts:
    - path: "docusaurus.config.ts"
      provides: "Favicon, colorMode, navbar links, social card configuration"
      contains: "favicon: 'img/favicon.png'"
    - path: "static/img/1nce-social-card.png"
      provides: "1NCE-branded Open Graph social card image"
    - path: "static/img/favicon.png"
      provides: "Official 1NCE 120x120 PNG favicon (already exists)"
  key_links:
    - from: "docusaurus.config.ts"
      to: "static/img/favicon.png"
      via: "favicon config field"
      pattern: "favicon.*favicon\\.png"
    - from: "docusaurus.config.ts"
      to: "static/img/1nce-social-card.png"
      via: "image config field"
      pattern: "image.*1nce-social-card"
    - from: "docusaurus.config.ts"
      to: "https://1nce.com"
      via: "navbar external link"
      pattern: "href.*1nce\\.com"
---

<objective>
Apply 1NCE branding to the Docusaurus site: switch favicon to the official 1NCE PNG, disable dark mode, add external navigation links, and replace the default social card.

Purpose: Make the site visually match the original 1NCE Developer Hub on ReadMe.com.
Output: Updated docusaurus.config.ts with all branding changes, new social card image, cleaned up favicon.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-branding-visual-alignment/08-CONTEXT.md
@.planning/phases/08-branding-visual-alignment/08-RESEARCH.md

<interfaces>
<!-- Current docusaurus.config.ts themeConfig structure (lines 172-234) -->
```typescript
themeConfig: {
  image: 'img/docusaurus-social-card.jpg',      // line 173 — change to 1nce-social-card.png
  colorMode: {                                    // lines 174-176 — replace block
    respectPrefersColorScheme: true,
  },
  navbar: {
    title: '1NCE Developer Hub',                 // line 178 — keep as-is (D-08)
    logo: {
      alt: '1NCE Logo',                          // line 180 — keep as-is
      src: 'img/1nce-logo.svg',                  // line 181 — keep as-is (already correct per research)
    },
    items: [                                      // lines 183-218 — append external links after existing items
      // ... 5 existing left-positioned items ...
    ],
  },
  // ...
} satisfies Preset.ThemeConfig;
```

<!-- Top-level config (line 10) -->
```typescript
favicon: 'img/favicon.ico',  // change to 'img/favicon.png'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docusaurus.config.ts for favicon, dark mode, navbar links, and social card</name>
  <files>docusaurus.config.ts</files>
  <read_first>
    - docusaurus.config.ts (current state of all config fields being modified)
    - .planning/phases/08-branding-visual-alignment/08-RESEARCH.md (exact values for URLs and config patterns)
  </read_first>
  <action>
Make four changes to docusaurus.config.ts:

1. **Favicon (BRAND-01, per D-01):** Change line 10 from `favicon: 'img/favicon.ico'` to `favicon: 'img/favicon.png'`. The existing `static/img/favicon.png` is already the official 1NCE 120x120 PNG (verified in research).

2. **Dark mode removal (BRAND-03, per D-03):** Replace the colorMode block (lines 174-176) from:
```typescript
colorMode: {
  respectPrefersColorScheme: true,
},
```
to:
```typescript
colorMode: {
  defaultMode: 'light',
  disableSwitch: true,
  respectPrefersColorScheme: false,
},
```

3. **External navbar links (NAV-01, per D-05/D-06/D-07):** Append three items to the end of the `navbar.items` array (after the existing Terms & Abbreviations item at line 217):
```typescript
{
  href: 'https://1nce.com',
  label: '1NCE Home',
  position: 'right',
  target: '_blank',
},
{
  href: 'https://portal.1nce.com/portal/shop/cart',
  label: '1NCE Shop',
  position: 'right',
  target: '_blank',
},
{
  href: 'https://portal.1nce.com/portal/customer/login',
  label: '1NCE Portal',
  position: 'right',
  target: '_blank',
},
```

4. **Social card (per D-09):** Change the `image` field (line 173) from `'img/docusaurus-social-card.jpg'` to `'img/1nce-social-card.png'`.

**Logo verification (BRAND-02, per D-02):** The existing `static/img/1nce-logo.svg` is already byte-identical to the 1nce.com corporate logo per research. The navbar `logo.src` already points to it. No change needed.

**Do NOT** modify any CSS files (per D-04 — dark CSS rules left as harmless dead code).
**Do NOT** change the navbar title (per D-08 — keep '1NCE Developer Hub').
  </action>
  <verify>
    <automated>cd /Users/Jan.Sulaiman/_Projects/docusaurus_poc && grep -c "favicon: 'img/favicon.png'" docusaurus.config.ts && grep -c "disableSwitch: true" docusaurus.config.ts && grep -c "respectPrefersColorScheme: false" docusaurus.config.ts && grep -c "1nce-social-card.png" docusaurus.config.ts && grep -c "https://1nce.com" docusaurus.config.ts && grep -c "portal.1nce.com/portal/shop/cart" docusaurus.config.ts && grep -c "portal.1nce.com/portal/customer/login" docusaurus.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - docusaurus.config.ts contains `favicon: 'img/favicon.png'` (not favicon.ico)
    - docusaurus.config.ts contains `defaultMode: 'light'`
    - docusaurus.config.ts contains `disableSwitch: true`
    - docusaurus.config.ts contains `respectPrefersColorScheme: false`
    - docusaurus.config.ts contains `image: 'img/1nce-social-card.png'`
    - docusaurus.config.ts contains navbar item with `href: 'https://1nce.com'` and `label: '1NCE Home'`
    - docusaurus.config.ts contains navbar item with `href: 'https://portal.1nce.com/portal/shop/cart'` and `label: '1NCE Shop'`
    - docusaurus.config.ts contains navbar item with `href: 'https://portal.1nce.com/portal/customer/login'` and `label: '1NCE Portal'`
    - All three external links have `position: 'right'` and `target: '_blank'`
    - docusaurus.config.ts still contains `title: '1NCE Developer Hub'` (unchanged per D-08)
    - docusaurus.config.ts still contains `src: 'img/1nce-logo.svg'` (unchanged per D-02)
    - No changes to src/css/custom.css (per D-04)
  </acceptance_criteria>
  <done>All four config changes applied: favicon points to PNG, dark mode disabled, three external navbar links added at right position, social card filename updated. Logo and title unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create social card image and clean up old favicon</name>
  <files>static/img/1nce-social-card.png, static/img/favicon.ico</files>
  <read_first>
    - static/img/1nce-logo.svg (logo SVG to embed in social card)
    - .planning/phases/08-branding-visual-alignment/08-RESEARCH.md (social card design guidance)
  </read_first>
  <action>
1. **Create social card (per D-09):** Generate a 1200x630 pixel PNG social card image at `static/img/1nce-social-card.png`. Design specifications:
   - Background: solid navy `#194a7d` (matches the site's `--ifm-navbar-background-color`)
   - Center the 1NCE logo (from `static/img/1nce-logo.svg`) in the upper-center area
   - Below the logo, add "Developer Hub" text in white (`#ffffff`) or teal (`#29abe2`)
   - Clean, minimal layout — this is for Open Graph/Twitter link previews

   Approach: Use a Node.js script with the `canvas` package (npm install canvas --save-dev) to programmatically generate the image. Alternatively, use a simple HTML-to-image approach or any available image generation method. The script should be a one-off build utility, not a permanent build dependency.

   If `canvas` is not available or too heavy, create a minimal SVG-based approach:
   - Create an SVG with the correct dimensions, navy background, embedded logo, and text
   - Convert to PNG using a CLI tool (e.g., `npx svg2png-cli` or `rsvg-convert` if available)

   The key requirement is that the file `static/img/1nce-social-card.png` exists as a valid 1200x630 PNG with 1NCE branding.

2. **Delete old favicon:** Remove `static/img/favicon.ico` (the Docusaurus default 32x27 dinosaur icon). The config no longer references it after Task 1.

3. **Delete old social card:** Remove `static/img/docusaurus-social-card.jpg` (the Docusaurus default). The config now references the new `1nce-social-card.png`.

4. **Build verification:** Run `cd /Users/Jan.Sulaiman/_Projects/docusaurus_poc && npx docusaurus build` to confirm the site builds successfully with all asset changes.
  </action>
  <verify>
    <automated>cd /Users/Jan.Sulaiman/_Projects/docusaurus_poc && test -f static/img/1nce-social-card.png && echo "social-card-exists" && test ! -f static/img/favicon.ico && echo "favicon-ico-deleted" && test ! -f static/img/docusaurus-social-card.jpg && echo "old-social-card-deleted" && file static/img/1nce-social-card.png | grep -i png && echo "is-valid-png"</automated>
  </verify>
  <acceptance_criteria>
    - File `static/img/1nce-social-card.png` exists and is a valid PNG image
    - File `static/img/1nce-social-card.png` is approximately 1200x630 pixels (Open Graph standard)
    - File `static/img/favicon.ico` does NOT exist (deleted)
    - File `static/img/docusaurus-social-card.jpg` does NOT exist (deleted)
    - `npx docusaurus build` completes without errors
  </acceptance_criteria>
  <done>1NCE-branded social card created at correct dimensions, old Docusaurus default favicon.ico and social card deleted, site builds successfully.</done>
</task>

</tasks>

<verification>
1. `grep "favicon: 'img/favicon.png'" docusaurus.config.ts` returns a match
2. `grep "disableSwitch: true" docusaurus.config.ts` returns a match
3. `grep "1nce.com" docusaurus.config.ts` returns matches for all three external links
4. `test -f static/img/1nce-social-card.png` succeeds
5. `test ! -f static/img/favicon.ico` succeeds
6. `npx docusaurus build` completes without errors
</verification>

<success_criteria>
- Browser tab will show 1NCE favicon (config points to existing favicon.png)
- No dark mode toggle visible (disableSwitch: true)
- OS dark preference ignored (respectPrefersColorScheme: false)
- Three external links in navbar right side: 1NCE Home, 1NCE Shop, 1NCE Portal
- Social sharing shows 1NCE-branded image
- Site builds successfully with all changes
</success_criteria>

<output>
After completion, create `.planning/phases/08-branding-visual-alignment/08-01-SUMMARY.md`
</output>
