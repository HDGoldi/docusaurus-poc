---
phase: 09-sidebar-consolidation-navigation-restructuring
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - docusaurus.config.ts
  - sidebars/platform.ts
  - sidebars/blueprints.ts
  - sidebars/terms.ts
  - package.json
autonomous: false
requirements: [NAV-03, NAV-04]

must_haves:
  truths:
    - "Navbar shows exactly 2 doc tabs (Documentation, API Explorer) plus 3 external links"
    - "No build errors referencing platform, blueprints, or terms plugin IDs"
    - "Visiting /platform/1nce-portal/portal-dashboard redirects to /docs/1nce-portal/portal-dashboard"
    - "Visiting /blueprints/blueprints-examples/quick-start redirects to /docs/blueprints-examples/quick-start"
    - "Visiting /terms/terms-abbreviations redirects to /docs/terms-abbreviations"
    - "API Explorer tab and all API endpoint pages still function"
    - "Documentation sidebar displays all 11 sections in correct order"
  artifacts:
    - path: "docusaurus.config.ts"
      provides: "Consolidated plugin config with redirects"
      contains: "plugin-client-redirects"
    - path: "package.json"
      provides: "Client redirects plugin dependency"
      contains: "plugin-client-redirects"
  key_links:
    - from: "docusaurus.config.ts plugins array"
      to: "docs/documentation/"
      via: "preset-classic docs.path"
      pattern: "path.*docs/documentation"
    - from: "docusaurus.config.ts plugins array"
      to: "plugin-client-redirects"
      via: "createRedirects function"
      pattern: "createRedirects"
    - from: "docusaurus.config.ts navbar items"
      to: "preset-classic docs"
      via: "docSidebar type referencing docsSidebar"
      pattern: "sidebarId.*docsSidebar"
---

<objective>
Remove the 3 obsolete plugin instances and their sidebar files, clean up the navbar to show only Documentation + API Explorer tabs, install and configure @docusaurus/plugin-client-redirects for old URL redirects, and verify the site builds successfully.

Purpose: Complete the plugin consolidation (NAV-03) and ensure old URLs redirect to new locations (NAV-04) so no external links break.
Output: A working Docusaurus build with unified navigation, correct sidebar, and client-side redirects.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-sidebar-consolidation-navigation-restructuring/09-CONTEXT.md
@.planning/phases/09-sidebar-consolidation-navigation-restructuring/09-RESEARCH.md
@.planning/phases/09-sidebar-consolidation-navigation-restructuring/09-01-SUMMARY.md

<interfaces>
Current docusaurus.config.ts structure (executor must read full file):

Lines 91-109: Plugin instances to REMOVE (platform, blueprints, terms)
  - id: platform, path: docs/platform, routeBasePath: /platform, sidebarPath: ./sidebars/platform.ts
  - id: blueprints, path: docs/blueprints, routeBasePath: /blueprints, sidebarPath: ./sidebars/blueprints.ts
  - id: terms, path: docs/terms, routeBasePath: /terms, sidebarPath: ./sidebars/terms.ts

Lines 110-116: API plugin instance to KEEP (id: api)
Lines 117-152: OpenAPI plugin to KEEP
Lines 153-167: Sass plugin + polyfill to KEEP

Lines 185-238: Navbar items array
  - Lines 186-191: Documentation tab (KEEP)
  - Lines 192-198: API Explorer tab (KEEP)
  - Lines 199-219: Platform, Blueprints, Terms tabs (REMOVE - these reference docsPluginId platform/blueprints/terms)
  - Lines 220-237: External links (KEEP)

Sidebar files:
  - sidebars/documentation.ts: docsSidebar using autogenerated (KEEP)
  - sidebars/api.ts (KEEP)
  - sidebars/platform.ts (DELETE)
  - sidebars/blueprints.ts (DELETE)
  - sidebars/terms.ts (DELETE)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove plugin instances, clean navbar, delete sidebar files, install and configure redirects plugin</name>
  <files>
    docusaurus.config.ts
    sidebars/platform.ts
    sidebars/blueprints.ts
    sidebars/terms.ts
    package.json
  </files>
  <read_first>
    docusaurus.config.ts
    sidebars/platform.ts
    sidebars/blueprints.ts
    sidebars/terms.ts
    package.json
  </read_first>
  <action>
Step 1 - Install @docusaurus/plugin-client-redirects (per D-08):
Run: npm install @docusaurus/plugin-client-redirects@3.9.2
Version MUST match installed @docusaurus/core (3.9.2) to avoid peer dependency conflicts.

Step 2 - Update docusaurus.config.ts, remove 3 plugin instances (per D-04):
Remove these 3 entries from the plugins array (currently lines 92-109):
  The entry with id: 'platform', path: 'docs/platform', routeBasePath: '/platform', sidebarPath: './sidebars/platform.ts'
  The entry with id: 'blueprints', path: 'docs/blueprints', routeBasePath: '/blueprints', sidebarPath: './sidebars/blueprints.ts'
  The entry with id: 'terms', path: 'docs/terms', routeBasePath: '/terms', sidebarPath: './sidebars/terms.ts'

Keep the API plugin instance (id: 'api'), openapi plugin, sass plugin, and polyfill function.

Step 3 - Add @docusaurus/plugin-client-redirects to plugins array (per D-08, D-09):
Add this entry to the plugins array (after the API plugin, before docusaurus-plugin-sass):

    ['@docusaurus/plugin-client-redirects', {
      createRedirects(existingPath) {
        // Platform sections: /platform/{section}/* was moved to /docs/{section}/*
        if (existingPath.match(/\/docs\/(1nce-os|1nce-portal|platform-services)(\/|$)/)) {
          return [existingPath.replace('/docs/', '/platform/')];
        }
        // Blueprints: /blueprints/blueprints-examples/* was moved to /docs/blueprints-examples/*
        if (existingPath.includes('/docs/blueprints-examples/')) {
          const slug = existingPath.split('/docs/blueprints-examples/').pop();
          const topLevelFiles = ['quectel-bg95-m3', 'quectel-ec25-ec21', 'recipes', 'sara-r410m', 'sim7000g', 'simcom-7020g-simcom800l'];
          if (topLevelFiles.some(f => slug?.startsWith(f))) {
            return [
              existingPath.replace('/docs/blueprints-examples/', '/blueprints/'),
            ];
          }
          return [
            existingPath.replace('/docs/blueprints-examples/', '/blueprints/blueprints-examples/'),
          ];
        }
        // Terms: /terms/terms-abbreviations was moved to /docs/terms-abbreviations
        if (existingPath.includes('/docs/terms-abbreviations')) {
          return ['/terms/terms-abbreviations'];
        }
        return undefined;
      },
    }],

Step 4 - Update navbar items (per D-12):
Replace the entire items array in themeConfig.navbar with exactly these 5 entries:

    items: [
      {
        type: 'docSidebar',
        sidebarId: 'docsSidebar',
        label: 'Documentation',
        position: 'left',
      },
      {
        type: 'doc',
        docId: 'index',
        label: 'API Explorer',
        position: 'left',
        docsPluginId: 'api',
      },
      {
        href: 'https://1nce.com',
        label: '1NCE Home',
        position: 'right',
        target: '_blank',
      },
      {
        href: 'https://portal.1nce.com/portal/shop/cart',
        label: '1NCE Shop',
        position: 'right',
        target: '_blank',
      },
      {
        href: 'https://portal.1nce.com/portal/customer/login',
        label: '1NCE Portal',
        position: 'right',
        target: '_blank',
      },
    ],

This removes the 3 doc tabs (1NCE Platform, Blueprints and Examples, Terms and Abbreviations) and keeps Documentation + API Explorer on left, 3 external links on right.

Step 5 - Delete obsolete sidebar files (per D-04):
  git rm sidebars/platform.ts
  git rm sidebars/blueprints.ts
  git rm sidebars/terms.ts
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - docusaurus.config.ts does NOT contain string "id: 'platform'" as a plugin entry
    - docusaurus.config.ts does NOT contain string "id: 'blueprints'" as a plugin entry
    - docusaurus.config.ts does NOT contain string "id: 'terms'" as a plugin entry
    - docusaurus.config.ts contains string "plugin-client-redirects"
    - docusaurus.config.ts contains string "createRedirects"
    - docusaurus.config.ts navbar items array has exactly 5 entries (Documentation, API Explorer, 1NCE Home, 1NCE Shop, 1NCE Portal)
    - docusaurus.config.ts does NOT contain string "docsPluginId: 'platform'"
    - docusaurus.config.ts does NOT contain string "docsPluginId: 'blueprints'"
    - docusaurus.config.ts does NOT contain string "docsPluginId: 'terms'"
    - sidebars/platform.ts does NOT exist
    - sidebars/blueprints.ts does NOT exist
    - sidebars/terms.ts does NOT exist
    - package.json contains string "plugin-client-redirects"
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Plugin instances removed, navbar cleaned to 2 doc tabs + 3 external links, sidebar files deleted, redirects plugin installed and configured. Site builds without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify sidebar order, navigation, and redirects</name>
  <files>N/A</files>
  <read_first>
    docusaurus.config.ts
  </read_first>
  <what-built>
    Consolidated all documentation into a single sidebar with 11 sections in the original ReadMe.com hub order. Reduced navbar from 5 doc tabs to 2 (Documentation + API Explorer). Added client-side redirects for all old URLs.
  </what-built>
  <action>
    Start the dev server with npm run start, then verify the sidebar order, navbar layout, redirect behavior, and API Explorer functionality as described in how-to-verify.
  </action>
  <how-to-verify>
    1. Run npm run start and open http://localhost:3000/docs/
    2. Sidebar order check (NAV-02): Verify the Documentation sidebar shows sections in this exact order:
       Introduction, 1NCE Portal, SIM Cards, MCP Server, Connectivity Services, Platform Services, Network Services, 1NCE OS, Troubleshooting, Blueprints and Examples, Terms and Abbreviations
    3. Navbar check (NAV-03): Verify only 2 doc tabs on the left (Documentation, API Explorer) and 3 external links on the right (1NCE Home, 1NCE Shop, 1NCE Portal). No Platform, Blueprints, or Terms tabs.
    4. API Explorer check: Click API Explorer tab, verify API endpoint pages load with working Try It panels
    5. Redirect checks (NAV-04): Test these old URLs redirect correctly:
       http://localhost:3000/platform/1nce-portal/portal-dashboard/ should redirect to /docs/1nce-portal/portal-dashboard/
       http://localhost:3000/blueprints/blueprints-examples/ should redirect to /docs/blueprints-examples/
       http://localhost:3000/terms/terms-abbreviations/ should redirect to /docs/terms-abbreviations/
    6. Content check: Click into a few pages in each section to verify content renders properly
  </how-to-verify>
  <verify>Human confirms sidebar order, navbar, redirects, and API Explorer all work correctly</verify>
  <acceptance_criteria>
    - Documentation sidebar has exactly 11 sections in the specified order
    - Navbar has exactly 5 items total
    - Old URLs redirect to new locations
    - API Explorer pages load with Try It panels
  </acceptance_criteria>
  <done>User has verified the sidebar order, navigation tabs, redirects, and API Explorer all function correctly.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- npm run build completes with exit code 0
- No warnings about missing plugin IDs in build output
- Build output contains redirect pages for /platform/*, /blueprints/*, /terms/* paths
- ls sidebars/ shows only api.ts and documentation.ts
- grep -c docsPluginId docusaurus.config.ts returns 1 (only the API reference)
</verification>

<success_criteria>
- Site builds successfully with no plugin-related errors
- Navbar shows exactly 5 items: Documentation, API Explorer, 1NCE Home, 1NCE Shop, 1NCE Portal
- Documentation sidebar has 11 sections in the correct order
- API Explorer pages still work with Try It panels
- Old URLs under /platform/*, /blueprints/*, /terms/* redirect to /docs/* equivalents
- No broken internal links in the build output
</success_criteria>

<output>
After completion, create .planning/phases/09-sidebar-consolidation-navigation-restructuring/09-02-SUMMARY.md
</output>
