---
phase: 10-crawler-foundation
verified: 2026-04-03T13:15:00Z
status: human_needed
score: 5/5 must-haves verified
human_verification:
  - test: "Run npm run build and confirm build/robots.txt and build/sitemap.xml are present"
    expected: "build/robots.txt exists with full content; build/sitemap.xml exists generated by preset-classic sitemap plugin"
    why_human: "Pre-existing build failure in src/theme/Root.tsx (useDocusaurusContext rendering issue from Phase 6 chat widget) prevents automated build verification. The static copy mechanism and sitemap plugin are standard Docusaurus behavior but cannot be confirmed without a passing build."
  - test: "Deploy updated CloudFormation stack and confirm .well-known/* passthrough in production"
    expected: "Requesting https://help.1nce.com/.well-known/test returns 403/404 from S3 (not a rewritten /index.html). Requesting https://help.1nce.com/robots.txt returns the robots.txt file content."
    why_human: "CloudFormation stack deployment required to activate the updated SpaRewriteFunction. Cannot verify live CloudFront behavior without deployment."
---

# Phase 10: Crawler Foundation Verification Report

**Phase Goal:** Search engines and AI crawlers can discover and index all site content through a properly served robots.txt with sitemap reference
**Verified:** 2026-04-03T13:15:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Visiting help.1nce.com/robots.txt returns a valid robots.txt with Sitemap directive | ✓ VERIFIED | `static/robots.txt` line 26: `Sitemap: https://help.1nce.com/sitemap.xml`. File placed in `static/` (Docusaurus copies to build root). sitemap.xml generated by `@docusaurus/preset-classic` sitemap plugin (confirmed active in preset source). |
| 2 | robots.txt includes explicit Allow directives for GPTBot, ClaudeBot, PerplexityBot, Google-Extended, CCBot, Bytespider | ✓ VERIFIED | All 6 bots present with individual `User-agent:` + `Allow: /` blocks at lines 8-25. `grep -c "User-agent:" static/robots.txt` returns 7 (1 wildcard + 6 named). |
| 3 | robots.txt disallows /search and /tags for the wildcard user-agent | ✓ VERIFIED | Lines 3-4: `Disallow: /search` and `Disallow: /tags` under the wildcard `User-agent: *` block. |
| 4 | Requesting /.well-known/* through CloudFront passes through without SPA rewrite | ✓ VERIFIED | `infra/cf-function.js` line 16: `if (uri.startsWith('/.well-known/')) { return request; }` as first check before any rewrite. `infra/template.yaml` lines 98-100: identical logic in `FunctionCode`. `SpaRewriteFunction` wired to both `ProdDistribution` (line 155) and `PreviewDistribution` (line 196). Needs deployment to take effect — see human verification. |
| 5 | Requesting .txt, .md, .json files through CloudFront returns actual file content | ✓ VERIFIED | The CF function's early-return for `.well-known/` and the existing `!uri.includes('.')` condition (which only rewrites paths with no extension) preserve passthrough for files with extensions. `.txt`, `.md`, `.json` all contain dots and will not be rewritten. Logic confirmed in both `cf-function.js` and `template.yaml`. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `static/robots.txt` | robots.txt served at site root | ✓ VERIFIED | 26 lines. Contains `Sitemap: https://help.1nce.com/sitemap.xml`, 7 User-agent blocks (wildcard + 6 AI bots), `Disallow: /search`, `Disallow: /tags`. No placeholder or TODO patterns. |
| `infra/cf-function.js` | CloudFront Function with .well-known passthrough | ✓ VERIFIED | 30 lines. `.well-known/` early return at line 16 as first conditional. Original SPA rewrite logic (endsWith slash, no extension) preserved at lines 21-27. No anti-patterns. |
| `infra/template.yaml` | CloudFormation template with updated inline CF function | ✓ VERIFIED | `FunctionCode` block (lines 94-107) contains identical `.well-known/` passthrough as first check. `SpaRewriteFunction` resource attached to both distributions. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `static/robots.txt` | `build/robots.txt` | Docusaurus static file copy | ✓ VERIFIED (logic) | File exists in `static/` directory matching existing pattern (`static/redirect-map.json` -> `build/redirect-map.json`). Build not run due to pre-existing Root.tsx issue; copy mechanism is standard Docusaurus. |
| `infra/cf-function.js` | `infra/template.yaml` | Identical function logic in both files | ✓ VERIFIED | Both files contain `uri.startsWith('/.well-known/')` as first check. Logic is functionally identical (comments differ, whitespace differs, but all three conditionals and return statements are identical). `template.yaml` is the deployed version. |

### Data-Flow Trace (Level 4)

Not applicable. Phase artifacts are static configuration files (robots.txt, CloudFront Function) — no dynamic data rendering.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| robots.txt has 7 User-agent blocks | `grep -c "User-agent:" static/robots.txt` | 7 | ✓ PASS |
| All 6 AI bots present | for-loop grep on each bot name | All 6 FOUND | ✓ PASS |
| Disallow rules correct | `grep "Disallow:" static/robots.txt` | `/search`, `/tags` | ✓ PASS |
| Sitemap directive present | `grep "Sitemap:" static/robots.txt` | `Sitemap: https://help.1nce.com/sitemap.xml` | ✓ PASS |
| .well-known check is first in cf-function.js | `grep -n "startsWith\|endsWith" infra/cf-function.js` | startsWith at line 16, endsWith at line 21 | ✓ PASS |
| .well-known check is first in template.yaml FunctionCode | awk line-number comparison | well-known at line 98, endsWith at line 101 | ✓ PASS |
| SpaRewriteFunction wired to both distributions | `grep -c "SpaRewriteFunction" infra/template.yaml` | 3 (definition + 2 FunctionARN refs) | ✓ PASS |
| npm run build produces build/robots.txt | `ls build/robots.txt` | build/ dir does not exist | ? SKIP — pre-existing build failure unrelated to this phase |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| CRAWL-01 | 10-01-PLAN.md | Site serves robots.txt at root with Sitemap reference to sitemap.xml | ✓ SATISFIED | `static/robots.txt` contains `Sitemap: https://help.1nce.com/sitemap.xml`; placed in Docusaurus `static/` for root serving |
| CRAWL-02 | 10-01-PLAN.md | robots.txt explicitly allows AI crawler user agents (GPTBot, ClaudeBot, PerplexityBot, Google-Extended, etc.) | ✓ SATISFIED | 6 named AI crawler blocks with `Allow: /` each: GPTBot, ClaudeBot, PerplexityBot, Google-Extended, CCBot, Bytespider |
| CRAWL-03 | 10-01-PLAN.md | CloudFront Function passes through requests for .txt, .md, .json files and .well-known/* paths without SPA rewrite | ✓ SATISFIED | `.well-known/` early return in both `cf-function.js` and `template.yaml`; `.txt/.md/.json` files pass through via existing `!uri.includes('.')` logic (files with extensions skip the rewrite); pending deployment |

No orphaned requirements: REQUIREMENTS.md maps only CRAWL-01, CRAWL-02, CRAWL-03 to Phase 10. All three are claimed by 10-01-PLAN.md and verified.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None found | — | — |

No TODO/FIXME/placeholder/stub patterns found in any of the three modified files.

### Human Verification Required

#### 1. Build output: robots.txt and sitemap.xml in build/

**Test:** Fix the pre-existing build failure in `src/theme/Root.tsx` (unrelated to this phase), then run `npm run build` and verify:
- `build/robots.txt` exists and matches `static/robots.txt` content
- `build/sitemap.xml` exists and is non-empty (generated by preset-classic)

**Expected:** Both files present. `build/robots.txt` has identical content to `static/robots.txt`. `build/sitemap.xml` contains URLs for all doc pages.

**Why human:** Pre-existing build failure in `src/theme/Root.tsx` prevents `npm run build` from completing. This failure predates Phase 10 (caused by Phase 6 chat widget). The static file copy and sitemap generation are standard Docusaurus behavior but cannot be confirmed without a passing build.

#### 2. Live CloudFront .well-known passthrough after stack deployment

**Test:** Deploy the updated CloudFormation stack (`infra/template.yaml`) to production, then:
- `curl -I https://help.1nce.com/robots.txt` — should return 200 with `Content-Type: text/plain`
- `curl -I https://help.1nce.com/.well-known/test` — should return 403 or 404 from S3 (not a redirect to index.html with 200)

**Expected:** robots.txt serves the file content. .well-known path does not get rewritten to index.html.

**Why human:** CloudFormation stack deployment required for the updated `SpaRewriteFunction` (with `.well-known/` passthrough) to take effect in production. Live CloudFront behavior cannot be verified without deployment.

### Gaps Summary

No gaps found. All five observable truths are verified by codebase evidence. All three required artifacts exist, are substantive (no stubs or placeholders), and are correctly wired. Requirements CRAWL-01, CRAWL-02, and CRAWL-03 are fully satisfied by the implementation.

Two items require human verification before the phase can be considered complete end-to-end:

1. The build output verification is blocked by a pre-existing build failure in `src/theme/Root.tsx` introduced in Phase 6 — not a regression from this phase.
2. CloudFront passthrough behavior requires stack deployment to verify in production — the code changes are correct and complete.

---
_Verified: 2026-04-03T13:15:00Z_
_Verifier: Claude (gsd-verifier)_
