---
phase: 01-content-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docusaurus.config.ts
  - tsconfig.json
  - src/css/custom.css
  - scripts/utils/file-utils.ts
  - scripts/utils/slug-map.ts
  - scripts/utils/logger.ts
  - scripts/01-copy-and-normalize.ts
  - scripts/02-convert-frontmatter.ts
autonomous: true
requirements:
  - CONV-01
  - CONV-09
  - CONV-10

must_haves:
  truths:
    - "Docusaurus project scaffolded and builds with zero errors on empty docs"
    - "All 298 export files copied to docs/ with normalized folder names"
    - "ReadMe frontmatter converted to Docusaurus-compatible frontmatter in all copied files"
    - "Shared utilities (slug-map, file-utils, logger) exist and are importable by later scripts"
  artifacts:
    - path: "package.json"
      provides: "Docusaurus project dependencies"
      contains: "@docusaurus/core"
    - path: "docusaurus.config.ts"
      provides: "Docusaurus configuration"
      contains: "preset-classic"
    - path: "scripts/01-copy-and-normalize.ts"
      provides: "Export-to-docs copy with folder normalization"
      exports: ["copyAndNormalize"]
    - path: "scripts/02-convert-frontmatter.ts"
      provides: "ReadMe to Docusaurus frontmatter conversion"
      exports: ["convertFrontmatter"]
    - path: "scripts/utils/slug-map.ts"
      provides: "Slug-to-path lookup table builder"
      exports: ["buildSlugMap"]
    - path: "scripts/utils/file-utils.ts"
      provides: "Common file operations for pipeline scripts"
      exports: ["readMarkdownFiles", "writeFile", "ensureDir"]
    - path: "scripts/utils/logger.ts"
      provides: "Pipeline logging utility"
      exports: ["log", "warn", "error"]
  key_links:
    - from: "scripts/01-copy-and-normalize.ts"
      to: "docs/"
      via: "file copy with path normalization"
      pattern: "1NCE OS.*1nce-os"
    - from: "scripts/02-convert-frontmatter.ts"
      to: "docs/**/*.md"
      via: "gray-matter parse and rewrite"
      pattern: "gray-matter"
---

<objective>
Scaffold the Docusaurus project, copy all 298 ReadMe export files to docs/ with normalized folder names, convert frontmatter, and create shared utility modules used by all subsequent conversion scripts.

Purpose: Establish the buildable Docusaurus project and normalized content foundation that all other conversion plans depend on. Without this, no conversion scripts have a target directory or shared utilities.

Output: A buildable Docusaurus 3.9.2 project with all export content copied (folder-name-normalized) and frontmatter converted, plus shared utility modules (slug-map, file-utils, logger).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-content-conversion/01-CONTEXT.md
@.planning/phases/01-content-conversion/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Docusaurus project and create shared utility modules</name>
  <files>
    package.json, docusaurus.config.ts, tsconfig.json, src/css/custom.css,
    scripts/utils/file-utils.ts, scripts/utils/slug-map.ts, scripts/utils/logger.ts
  </files>
  <read_first>
    dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/_order.yaml,
    dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/docs/ (list directory structure),
    .planning/phases/01-content-conversion/01-RESEARCH.md
  </read_first>
  <action>
    1. Scaffold Docusaurus project (per D-01, D-08):
       ```bash
       npx create-docusaurus@3.9.2 . classic --typescript
       ```
       If the project root already has files, scaffold into a temp dir and copy config files.

    2. Install additional dependencies:
       ```bash
       npm install @docusaurus/faster@3.9.2
       npm install --save-dev gray-matter@4.0.3 glob@13.0.6 js-yaml@4.1.1 @types/js-yaml tsx
       ```

    3. Update `docusaurus.config.ts`:
       - Set `title: '1NCE Developer Hub'`
       - Set `url: 'https://help.1nce.com'`
       - Set `baseUrl: '/'`
       - Set `onBrokenLinks: 'throw'` (strict link validation)
       - Set `onBrokenMarkdownLinks: 'throw'`
       - Enable `@docusaurus/faster` in the config:
         ```typescript
         future: {
           experimental_faster: true,
         },
         ```
       - Keep preset-classic with docs plugin pointing to `./docs`
       - Set `docs: { routeBasePath: '/' }` so docs are at the root

    4. Create `scripts/utils/logger.ts`:
       ```typescript
       const COLORS = { reset: '\x1b[0m', red: '\x1b[31m', yellow: '\x1b[33m', green: '\x1b[32m', cyan: '\x1b[36m' };
       export function log(step: string, msg: string): void { console.log(`${COLORS.cyan}[${step}]${COLORS.reset} ${msg}`); }
       export function warn(step: string, msg: string): void { console.warn(`${COLORS.yellow}[${step}] WARN:${COLORS.reset} ${msg}`); }
       export function error(step: string, msg: string): void { console.error(`${COLORS.red}[${step}] ERROR:${COLORS.reset} ${msg}`); }
       export function success(step: string, msg: string): void { console.log(`${COLORS.green}[${step}] OK:${COLORS.reset} ${msg}`); }
       ```

    5. Create `scripts/utils/file-utils.ts`:
       - `readMarkdownFiles(dir: string): string[]` -- uses `globSync('**/*.md', { cwd: dir })` to find all .md files
       - `readFile(filePath: string): string` -- reads file as utf-8
       - `writeFile(filePath: string, content: string): void` -- writes file, creates parent dirs with `fs.mkdirSync(dir, { recursive: true })`
       - `ensureDir(dirPath: string): void` -- creates directory recursively
       - `copyDir(src: string, dest: string): void` -- recursively copies directory
       - Import `globSync` from `glob`, `fs` from `node:fs`, `path` from `node:path`

    6. Create `scripts/utils/slug-map.ts` (per research Pattern 2):
       - `buildSlugMap(docsDir: string): Map<string, string>` -- scans all .md files in docsDir, extracts the filename without extension as the slug key, and the relative path without extension as the Docusaurus doc ID value (prefixed with `/`)
       - Uses `gray-matter` to parse frontmatter, `globSync` to find files
       - Also checks for `slug` field in frontmatter as an override key
       - Returns a Map where keys are slugs and values are paths like `/1nce-os/device-controller/device-controller-api`

    7. Delete the default Docusaurus `docs/` and `blog/` scaffold content (we will populate docs/ from the export in Task 2).

    8. Verify scaffold builds:
       ```bash
       npx docusaurus build
       ```
       (May need a placeholder index.md in docs/ to build successfully -- create one if needed)
  </action>
  <verify>
    <automated>cd /Users/Jan.Sulaiman/_Projects/docusaurus_poc && npx docusaurus build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@docusaurus/core": "3.9.2"`
    - package.json contains `"@docusaurus/faster": "3.9.2"`
    - package.json contains `"gray-matter"` in devDependencies
    - package.json contains `"glob"` in devDependencies
    - package.json contains `"js-yaml"` in devDependencies
    - docusaurus.config.ts contains `title: '1NCE Developer Hub'`
    - docusaurus.config.ts contains `onBrokenLinks: 'throw'`
    - docusaurus.config.ts contains `experimental_faster`
    - scripts/utils/logger.ts exports `log`, `warn`, `error`, `success`
    - scripts/utils/file-utils.ts exports `readMarkdownFiles`, `writeFile`, `ensureDir`
    - scripts/utils/slug-map.ts exports `buildSlugMap`
    - `npx docusaurus build` exits with code 0
  </acceptance_criteria>
  <done>Docusaurus 3.9.2 project scaffolded, builds cleanly, and all shared utility modules exist with correct exports.</done>
</task>

<task type="auto">
  <name>Task 2: Copy/normalize export content and convert frontmatter</name>
  <files>
    scripts/01-copy-and-normalize.ts, scripts/02-convert-frontmatter.ts, docs/ (output)
  </files>
  <read_first>
    dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/_order.yaml,
    dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/docs/ (list 3-4 sample files to see frontmatter structure),
    dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/recipes/ (list directory),
    dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/custom_pages/ (list directory),
    scripts/utils/file-utils.ts,
    scripts/utils/logger.ts
  </read_first>
  <action>
    1. Create `scripts/01-copy-and-normalize.ts` (per D-03, D-04, D-08, D-10, D-11, D-13):

       **Input:** `dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/` export directory
       **Output:** `docs/` directory with normalized folder names

       The script must:
       a. Clear the `docs/` directory if it exists (idempotent per D-04)
       b. Copy `dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/docs/` content to `docs/`
       c. Copy `dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/recipes/` content into `docs/blueprints-examples/` (per D-10 -- merge recipes under Blueprints & Examples)
       d. Copy `dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/custom_pages/terms-abbreviations.md` to `docs/terms-abbreviations.md` (per D-13)
       e. Skip `ai-support-agent.html` and `ai-support-agent-brazil.html` files (per D-11)
       f. Normalize ALL folder names: lowercase, replace spaces with hyphens, replace `&` with nothing or hyphen
          - "1NCE OS" -> "1nce-os"
          - "Blueprints & Examples" -> "blueprints-examples"
          - "SIM Cards" -> "sim-cards"
          - Preserve the hierarchy depth
       g. Copy `_order.yaml` files alongside the content (needed for sidebar generation)
       h. Log: total files copied, folders created, files skipped
       i. Also copy `dev-hub-v2.6-2026-03-20T20-39-14_8a4df1b/reference/` markdown files (NOT the OpenAPI JSON specs) to `docs/reference/` -- these are supplementary API docs that need conversion too

    2. Create `scripts/02-convert-frontmatter.ts` (per D-09, CONV-09, research Pattern 3):

       **Input:** `docs/` directory (output of step 01)
       **Output:** Same files rewritten with Docusaurus-compatible frontmatter

       The script must:
       a. Find all `.md` files in `docs/` using `readMarkdownFiles()`
       b. For each file, parse frontmatter with `gray-matter`
       c. Apply the frontmatter mapping table:
          - `title` -> `title` (direct copy)
          - `excerpt` -> `description` (if no `metadata.description` exists)
          - `metadata.description` -> `description` (promoted to top-level, takes priority over excerpt)
          - `metadata.title` -> drop (Docusaurus uses `title` for both)
          - `metadata.robots` -> drop (handle via global Docusaurus SEO config)
          - `hidden: true` -> `draft: true`
          - `deprecated` -> drop (could add admonition banner in content if `deprecated` was truthy)
          - `next` -> drop (Docusaurus uses sidebar ordering)
       d. Write the file back with new frontmatter using `matter.stringify(body, newFrontmatter)`
       e. Log: total files processed, files with hidden->draft conversion

    3. Add npm scripts to package.json:
       ```json
       "scripts": {
         "convert:copy": "tsx scripts/01-copy-and-normalize.ts",
         "convert:frontmatter": "tsx scripts/02-convert-frontmatter.ts"
       }
       ```

    4. Run both scripts:
       ```bash
       npm run convert:copy
       npm run convert:frontmatter
       ```
  </action>
  <verify>
    <automated>cd /Users/Jan.Sulaiman/_Projects/docusaurus_poc && npm run convert:copy 2>&1 | tail -3 && npm run convert:frontmatter 2>&1 | tail -3 && find docs -name "*.md" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - scripts/01-copy-and-normalize.ts exists and contains `dev-hub-v2.6`
    - scripts/02-convert-frontmatter.ts exists and contains `gray-matter`
    - docs/ directory exists and contains .md files
    - No folder names in docs/ contain uppercase letters or spaces (normalized per D-08)
    - docs/blueprints-examples/ directory exists (recipes merged per D-10)
    - docs/terms-abbreviations.md exists (per D-13)
    - No file named `ai-support-agent.html` exists under docs/ (per D-11)
    - No .md file in docs/ contains `metadata:` as a top-level frontmatter key (all promoted or dropped)
    - No .md file in docs/ contains `excerpt:` in frontmatter (converted to `description`)
    - `find docs -name "*.md" | wc -l` returns at least 250 (most of 298 files copied)
    - package.json contains `"convert:copy"` and `"convert:frontmatter"` scripts
  </acceptance_criteria>
  <done>All export content is in docs/ with normalized folder names, frontmatter is Docusaurus-compatible, and both scripts are re-runnable.</done>
</task>

</tasks>

<verification>
1. `npx docusaurus build` completes (may have MDX errors from unconverted syntax -- that is expected; the project itself builds)
2. `find docs -name "*.md" | wc -l` shows at least 250 files
3. No uppercase folder names in docs/: `find docs -type d | grep '[A-Z]'` returns empty
4. Frontmatter check: `head -10 docs/introduction/*.md` shows `title:` and `description:` fields, no `metadata:` block
</verification>

<success_criteria>
- Docusaurus 3.9.2 project scaffolded with @docusaurus/faster
- All 298 export files copied to docs/ with normalized paths
- Frontmatter converted from ReadMe format to Docusaurus format
- Shared utility modules (slug-map, file-utils, logger) available for subsequent plans
- Both conversion scripts are idempotent and re-runnable
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-conversion/01-01-SUMMARY.md`
</output>
