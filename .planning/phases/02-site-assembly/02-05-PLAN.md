---
phase: 02-site-assembly
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docusaurus.config.ts
  - docs/api/authorization/_category_.json
  - docs/api/sim-management/_category_.json
  - docs/api/order-management/_category_.json
  - docs/api/product-information/_category_.json
  - docs/api/support-management/_category_.json
  - docs/api/1nce-os/_category_.json
autonomous: false
requirements: [NAV-02, API-01]
gap_closure: true

must_haves:
  truths:
    - "Clicking API Explorer navbar tab loads the API landing/introduction page at /api"
    - "API Explorer sidebar categories appear in order: Authorization, SIM Management, Order Management, Product Information, Support Management, 1NCE OS"
    - "CORS limitation on Try It panel is documented as known limitation"
  artifacts:
    - path: "docusaurus.config.ts"
      provides: "Navbar API Explorer item pointing to index doc"
      contains: "docId"
    - path: "docs/api/authorization/_category_.json"
      provides: "Category ordering for Authorization"
      contains: "position"
    - path: "docs/api/sim-management/_category_.json"
      provides: "Category ordering for SIM Management"
    - path: "docs/api/order-management/_category_.json"
      provides: "Category ordering for Order Management"
    - path: "docs/api/product-information/_category_.json"
      provides: "Category ordering for Product Information"
    - path: "docs/api/support-management/_category_.json"
      provides: "Category ordering for Support Management"
    - path: "docs/api/1nce-os/_category_.json"
      provides: "Category ordering for 1NCE OS"
  key_links:
    - from: "docusaurus.config.ts"
      to: "docs/api/index.md"
      via: "navbar item docId: index"
      pattern: "docId.*index"
---

<objective>
Close UAT gaps 1 and 2 from phase 02 user acceptance testing: fix API Explorer navbar landing page and API sidebar category ordering.

Purpose: The API Explorer tab currently lands on the wrong page (1nce-os instead of the intro page) and sidebar categories are alphabetically sorted instead of logically ordered. Gap 3 (CORS) is accepted as a known limitation. Gap 4 (redirect-map.json) is a false positive.
Output: Corrected navbar config, six _category_.json files, updated UAT status.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-site-assembly/02-UAT.md

<interfaces>
<!-- Current navbar API Explorer item in docusaurus.config.ts (line 184-189) -->
```typescript
{
  type: 'docSidebar',
  sidebarId: 'apiSidebar',
  label: 'API Explorer',
  position: 'left',
  docsPluginId: 'api',
},
```

<!-- Current API sidebar in sidebars/api.ts -->
```typescript
const sidebars: SidebarsConfig = {
  apiSidebar: [{type: 'autogenerated', dirName: '.'}],
};
```

<!-- docs/api/index.md exists with slug: / and title: API Explorer -->
<!-- docs/api/ subdirectories: 1nce-os, authorization, order-management, product-information, sim-management, support-management -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix API Explorer navbar landing page and sidebar ordering</name>
  <files>
    docusaurus.config.ts,
    docs/api/authorization/_category_.json,
    docs/api/sim-management/_category_.json,
    docs/api/order-management/_category_.json,
    docs/api/product-information/_category_.json,
    docs/api/support-management/_category_.json,
    docs/api/1nce-os/_category_.json
  </files>
  <action>
**Gap 1 fix — API Explorer navbar landing page (UAT test 10):**

In `docusaurus.config.ts`, change the API Explorer navbar item (around line 184-189) from:
```typescript
{
  type: 'docSidebar',
  sidebarId: 'apiSidebar',
  label: 'API Explorer',
  position: 'left',
  docsPluginId: 'api',
}
```
to:
```typescript
{
  type: 'doc',
  docId: 'index',
  label: 'API Explorer',
  position: 'left',
  docsPluginId: 'api',
}
```

This makes the navbar link resolve directly to `docs/api/index.md` (which has `slug: /`) instead of the first alphabetical leaf doc in the autogenerated sidebar.

**Gap 2 fix — API sidebar category ordering (UAT test 11):**

Create `_category_.json` in each of the 6 API subdirectories with the correct position and label:

1. `docs/api/authorization/_category_.json`: `{"label": "Authorization", "position": 1}`
2. `docs/api/sim-management/_category_.json`: `{"label": "SIM Management", "position": 2}`
3. `docs/api/order-management/_category_.json`: `{"label": "Order Management", "position": 3}`
4. `docs/api/product-information/_category_.json`: `{"label": "Product Information", "position": 4}`
5. `docs/api/support-management/_category_.json`: `{"label": "Support Management", "position": 5}`
6. `docs/api/1nce-os/_category_.json`: `{"label": "1NCE OS", "position": 6}`

Do NOT modify `sidebars/api.ts` — the autogenerated sidebar will pick up _category_.json ordering automatically.

**Gap 3 — CORS on Try It panel (UAT test 12):**

This is an accepted limitation. The 1NCE API (`api.1nce.com`) does not return CORS headers, which is outside the scope of this docs migration. Add a visible warning to `docs/api/index.md` noting that the Try It panel may encounter CORS errors and users should copy the generated curl/code snippets to execute locally. Check if a warning already exists in index.md — if so, ensure it mentions CORS specifically.

**Gap 4 — redirect-map.json (UAT test 13):**

No action needed. This was a false positive — the file is valid JSON on disk. The dev server SPA fallback intercepted the request during testing.
  </action>
  <verify>
    <automated>cd /Users/Jan.Sulaiman/_Projects/docusaurus_poc && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - API Explorer navbar tab navigates to /api (the index landing page), not /api/1nce-os/*
    - API sidebar categories appear in order: Authorization (1), SIM Management (2), Order Management (3), Product Information (4), Support Management (5), 1NCE OS (6)
    - docs/api/index.md contains a CORS limitation note
    - Build completes without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify API Explorer fixes</name>
  <files>none</files>
  <action>Human verifies the API Explorer navbar and sidebar fixes visually in the browser.</action>
  <what-built>Fixed API Explorer navbar landing page (now points to intro page instead of 1nce-os), added category ordering to all 6 API subdirectories, and documented CORS limitation.</what-built>
  <how-to-verify>
    1. Run `npm start` (or reuse running dev server)
    2. Click the "API Explorer" tab in the navbar
    3. Verify it lands on the API introduction page (Welcome to the 1NCE API) at /api, NOT on a 1nce-os endpoint page
    4. Check the API Explorer sidebar — categories should appear in order: Authorization, SIM Management, Order Management, Product Information, Support Management, 1NCE OS
    5. Verify the API intro page has a note about CORS/Try It limitations
  </how-to-verify>
  <verify>Human approval</verify>
  <done>User confirms all 3 UAT issues are resolved or accepted</done>
  <resume-signal>Type "approved" or describe any remaining issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Navbar API Explorer item resolves to /api (index page)
- Sidebar categories render in specified order (not alphabetical)
</verification>

<success_criteria>
UAT tests 10 and 11 now pass. UAT test 12 is documented as accepted limitation. UAT test 13 confirmed as false positive.
</success_criteria>

<output>
After completion, create `.planning/phases/02-site-assembly/02-05-SUMMARY.md`
</output>
