---
phase: 03-infrastructure-and-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/template.yaml
  - infra/cf-function.js
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "CloudFormation template defines all AWS resources needed for production and preview hosting"
    - "S3 buckets are private, accessible only via CloudFront OAC"
    - "CloudFront Function rewrites SPA paths to index.html"
    - "ACM certificate is provisioned in us-east-1 with DNS validation"
    - "Route 53 alias record points help.1nce.com to CloudFront"
  artifacts:
    - path: "infra/template.yaml"
      provides: "CloudFormation template with all AWS resources"
      contains: "AWS::CloudFront::Distribution"
    - path: "infra/cf-function.js"
      provides: "CloudFront Function source for SPA routing"
      contains: "index.html"
  key_links:
    - from: "infra/template.yaml"
      to: "infra/cf-function.js"
      via: "FunctionCode inline or reference"
      pattern: "cf-function"
    - from: "infra/template.yaml ProdBucketPolicy"
      to: "infra/template.yaml ProdDistribution"
      via: "GetAtt Distribution ARN in bucket policy condition"
      pattern: "GetAtt.*ProdDistribution"
---

<objective>
Create the CloudFormation template and CloudFront Function that define all AWS infrastructure for hosting the 1NCE Developer Hub at help.1nce.com.

Purpose: Provide a single, version-controlled IaC template (per D-14) that provisions S3 buckets, CloudFront distributions with OAC, ACM certificate, Route 53 DNS, and SPA routing -- everything needed to serve the Docusaurus static site in production and preview environments.

Output: `infra/template.yaml` (CloudFormation) and `infra/cf-function.js` (CloudFront Function source)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-infrastructure-and-deployment/03-CONTEXT.md
@.planning/phases/03-infrastructure-and-deployment/03-RESEARCH.md

@docusaurus.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CloudFront Function for SPA routing</name>
  <files>infra/cf-function.js</files>
  <read_first>
    - .planning/phases/03-infrastructure-and-deployment/03-RESEARCH.md (Pattern 3: CloudFront Function for SPA Routing)
    - docusaurus.config.ts (verify trailingSlash setting -- currently undefined, meaning /page/index.html structure)
  </read_first>
  <action>
Create `infra/cf-function.js` with a CloudFront Function (runtime: cloudfront-js-2.0) that handles Docusaurus SPA routing per D-06 and D-07.

The function is a viewer-request handler with this exact logic:
1. If URI ends with `/`, append `index.html` (e.g., `/docs/` becomes `/docs/index.html`)
2. If URI has no file extension (no `.` in the last path segment), append `/index.html` (e.g., `/docs/page` becomes `/docs/page/index.html`)
3. Otherwise, pass through unchanged (e.g., `/assets/js/main.abc123.js` stays as-is)

Implementation:
```javascript
// CloudFront Function: SPA routing for Docusaurus
// Runtime: cloudfront-js-2.0
// Attached as viewer-request to both prod and preview distributions
async function handler(event) {
    var request = event.request;
    var uri = request.uri;

    // URI ends with slash: append index.html
    if (uri.endsWith('/')) {
        request.uri += 'index.html';
    }
    // URI has no file extension: append /index.html
    else if (!uri.includes('.')) {
        request.uri += '/index.html';
    }

    return request;
}
```

This file is referenced by the CloudFormation template (Task 2) as inline FunctionCode.
  </action>
  <verify>
    <automated>test -f infra/cf-function.js && grep -q "index.html" infra/cf-function.js && grep -q "handler" infra/cf-function.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - infra/cf-function.js exists
    - File contains `async function handler(event)`
    - File contains `request.uri += 'index.html'` (slash case)
    - File contains `request.uri += '/index.html'` (no-extension case)
    - File contains `uri.endsWith('/')` check
    - File contains `!uri.includes('.')` check
    - File contains comment referencing `cloudfront-js-2.0` runtime
  </acceptance_criteria>
  <done>CloudFront Function source file exists with SPA routing logic handling both trailing-slash and no-extension cases</done>
</task>

<task type="auto">
  <name>Task 2: Create CloudFormation template with all AWS resources</name>
  <files>infra/template.yaml</files>
  <read_first>
    - infra/cf-function.js (the CF Function source created in Task 1 -- inline into template)
    - .planning/phases/03-infrastructure-and-deployment/03-RESEARCH.md (Pattern 1: CloudFormation Template Design, Pattern 4: S3 Bucket Policy, IAM Role Trust Policy, IAM Permissions Policy, Pitfalls 1-5)
    - .planning/phases/03-infrastructure-and-deployment/03-CONTEXT.md (all D-* decisions)
    - docusaurus.config.ts (verify url is https://help.1nce.com)
  </read_first>
  <action>
Create `infra/template.yaml` -- a single CloudFormation template per D-14 that provisions all AWS resources. Deploy the entire stack in us-east-1 (ACM cert requirement for CloudFront; S3 region does not affect CF performance).

**Parameters section:**
- `DomainName` (String, Default: `help.1nce.com`) -- custom domain
- `HostedZoneId` (String, no default) -- Route 53 hosted zone ID for help.1nce.com
- `GitHubOrg` (String, no default) -- GitHub organization name for OIDC trust policy
- `GitHubRepo` (String, no default) -- GitHub repository name for OIDC trust policy

**Resources to create (in this order to avoid circular dependencies):**

1. **ProdBucket** (`AWS::S3::Bucket`):
   - BucketName: `1nce-developer-hub-prod` (per D-15)
   - No `WebsiteConfiguration` (per anti-pattern: use REST endpoint, not website endpoint)
   - `PublicAccessBlockConfiguration` with all four settings set to `true`

2. **PreviewBucket** (`AWS::S3::Bucket`):
   - BucketName: `1nce-developer-hub-preview` (per D-15)
   - No `WebsiteConfiguration`
   - `PublicAccessBlockConfiguration` with all four settings set to `true`
   - `LifecycleConfiguration`: expire objects after 30 days (cleanup stale previews)

3. **OriginAccessControl** (`AWS::CloudFront::OriginAccessControl`):
   - `OriginAccessControlOriginType: s3`
   - `SigningBehavior: always`
   - `SigningProtocol: sigv4`
   - Name: `!Sub "${AWS::StackName}-oac"`

4. **SpaRewriteFunction** (`AWS::CloudFront::Function`):
   - `AutoPublish: true` (per Pitfall 4 -- must be LIVE stage)
   - `FunctionConfig.Runtime: cloudfront-js-2.0`
   - `FunctionConfig.Comment: "SPA routing rewrite for Docusaurus"`
   - `FunctionCode`: inline the content from `infra/cf-function.js`
   - Name: `!Sub "${AWS::StackName}-spa-rewrite"`

5. **Certificate** (`AWS::CertificateManager::Certificate`):
   - `DomainName: !Ref DomainName`
   - `ValidationMethod: DNS`
   - `DomainValidationOptions` with `HostedZoneId: !Ref HostedZoneId` (per D-17, D-13 -- DNS already in Route 53)

6. **ProdDistribution** (`AWS::CloudFront::Distribution`):
   - `Enabled: true`
   - `Aliases: [!Ref DomainName]`
   - Origin: `DomainName: !GetAtt ProdBucket.RegionalDomainName`, `S3OriginConfig.OriginAccessIdentity: ""`, `OriginAccessControlId: !GetAtt OriginAccessControl.Id` (per D-09)
   - `DefaultCacheBehavior`:
     - `ViewerProtocolPolicy: redirect-to-https`
     - `CachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6` (CachingOptimized -- per D-08 aggressive caching)
     - `Compress: true`
     - `FunctionAssociations`: viewer-request -> `!GetAtt SpaRewriteFunction.FunctionMetadata.FunctionARN`
   - `ViewerCertificate`: `AcmCertificateArn: !Ref Certificate`, `SslSupportMethod: sni-only`, `MinimumProtocolVersion: TLSv1.2_2021`
   - `DefaultRootObject: index.html`
   - `CustomErrorResponses`:
     - 403 -> 404.html with ResponseCode 404, ErrorCachingMinTTL 300
     - 404 -> 404.html with ResponseCode 404, ErrorCachingMinTTL 300
   - `HttpVersion: http2and3`
   - `IPV6Enabled: true`

7. **PreviewDistribution** (`AWS::CloudFront::Distribution`):
   - `Enabled: true`
   - No `Aliases` (uses default `*.cloudfront.net` domain per D-16)
   - Origin: `DomainName: !GetAtt PreviewBucket.RegionalDomainName`, same OAC config
   - `DefaultCacheBehavior`:
     - `ViewerProtocolPolicy: redirect-to-https`
     - `CachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6`
     - `Compress: true`
     - `FunctionAssociations`: viewer-request -> `!GetAtt SpaRewriteFunction.FunctionMetadata.FunctionARN`
   - No ViewerCertificate (uses default CloudFront cert)
   - `DefaultRootObject: index.html`
   - `CustomErrorResponses`: same as prod
   - `HttpVersion: http2and3`

8. **ProdBucketPolicy** (`AWS::S3::BucketPolicy`):
   - Bucket: `!Ref ProdBucket`
   - Statement: Allow `s3:GetObject` to `cloudfront.amazonaws.com` principal with condition `StringEquals AWS:SourceArn: !Sub "arn:aws:cloudfront::${AWS::AccountId}:distribution/${ProdDistribution}"`
   - (Separate resource to avoid circular dependency per Pitfall 3)

9. **PreviewBucketPolicy** (`AWS::S3::BucketPolicy`):
   - Same pattern as ProdBucketPolicy but for PreviewBucket and PreviewDistribution

10. **DnsRecord** (`AWS::Route53::RecordSet`):
    - `HostedZoneId: !Ref HostedZoneId`
    - `Name: !Ref DomainName`
    - `Type: A`
    - `AliasTarget`: `DNSName: !GetAtt ProdDistribution.DomainName`, `HostedZoneId: Z2FDTNDATAQYW2` (CloudFront's fixed hosted zone ID)
    - Per D-10 and D-13

11. **DnsRecordIPv6** (`AWS::Route53::RecordSet`):
    - Same as DnsRecord but `Type: AAAA`

12. **GitHubOidcProvider** (`AWS::IAM::OIDCProvider`):
    - `Url: https://token.actions.githubusercontent.com`
    - `ClientIdList: [sts.amazonaws.com]`
    - `ThumbprintList: [6938fd4d98bab03faadb97b34396831e3780aea1]` (GitHub's OIDC thumbprint)

13. **GitHubActionsRole** (`AWS::IAM::Role`):
    - `AssumeRolePolicyDocument`: trust policy allowing `sts:AssumeRoleWithWebIdentity` from the OIDC provider, with conditions:
      - `StringEquals token.actions.githubusercontent.com:aud: sts.amazonaws.com`
      - `StringLike token.actions.githubusercontent.com:sub: !Sub "repo:${GitHubOrg}/${GitHubRepo}:*"`
    - `Policies` inline policy with:
      - S3: `s3:PutObject`, `s3:DeleteObject`, `s3:ListBucket`, `s3:GetObject` on both bucket ARNs and their `/*` paths
      - CloudFront: `cloudfront:CreateInvalidation` on `!Sub "arn:aws:cloudfront::${AWS::AccountId}:distribution/${ProdDistribution}"` and PreviewDistribution

**Outputs section:**
- `ProdDistributionId`: `!Ref ProdDistribution`
- `ProdDistributionDomain`: `!GetAtt ProdDistribution.DomainName`
- `PreviewDistributionId`: `!Ref PreviewDistribution`
- `PreviewDistributionDomain`: `!GetAtt PreviewDistribution.DomainName`
- `GitHubActionsRoleArn`: `!GetAtt GitHubActionsRole.Arn`
- `ProdBucketName`: `!Ref ProdBucket`
- `PreviewBucketName`: `!Ref PreviewBucket`

Add a `Description` at the top: "AWS infrastructure for 1NCE Developer Hub (help.1nce.com) - S3, CloudFront, ACM, Route 53, IAM OIDC"
  </action>
  <verify>
    <automated>grep -c "AWS::S3::Bucket\|AWS::CloudFront::Distribution\|AWS::CloudFront::OriginAccessControl\|AWS::CloudFront::Function\|AWS::CertificateManager::Certificate\|AWS::Route53::RecordSet\|AWS::IAM::OIDCProvider\|AWS::IAM::Role\|AWS::S3::BucketPolicy" infra/template.yaml | xargs test 11 -le && echo "PASS: all resource types present" || echo "FAIL: missing resource types"</automated>
  </verify>
  <acceptance_criteria>
    - infra/template.yaml exists and is valid YAML
    - Contains parameter `DomainName` with default `help.1nce.com`
    - Contains parameter `HostedZoneId` with no default
    - Contains parameter `GitHubOrg` with no default
    - Contains parameter `GitHubRepo` with no default
    - Contains `AWS::S3::Bucket` resource named ProdBucket with BucketName `1nce-developer-hub-prod`
    - Contains `AWS::S3::Bucket` resource named PreviewBucket with BucketName `1nce-developer-hub-preview`
    - ProdBucket has PublicAccessBlockConfiguration with all four blocks true
    - PreviewBucket has LifecycleConfiguration with 30-day expiration
    - Contains `AWS::CloudFront::OriginAccessControl` with `SigningBehavior: always` and `SigningProtocol: sigv4`
    - Contains `AWS::CloudFront::Function` with `AutoPublish: true` and `Runtime: cloudfront-js-2.0`
    - Contains `AWS::CertificateManager::Certificate` with `ValidationMethod: DNS`
    - Contains `AWS::CloudFront::Distribution` for prod with `Aliases` containing `!Ref DomainName`
    - Contains `AWS::CloudFront::Distribution` for preview WITHOUT `Aliases`
    - Both distributions have `FunctionAssociations` for viewer-request
    - Contains `AWS::S3::BucketPolicy` for both buckets (separate resources, not inline)
    - Bucket policies use `cloudfront.amazonaws.com` principal with SourceArn condition
    - Contains `AWS::Route53::RecordSet` type A and AAAA with AliasTarget
    - Contains `AWS::IAM::OIDCProvider` with `token.actions.githubusercontent.com`
    - Contains `AWS::IAM::Role` with trust policy using `StringLike` on `sub` claim
    - IAM policy includes `s3:PutObject`, `s3:DeleteObject`, `s3:ListBucket`, `cloudfront:CreateInvalidation`
    - Outputs section includes `ProdDistributionId`, `GitHubActionsRoleArn`, `ProdBucketName`
    - Template has `AWSTemplateFormatVersion` and `Description`
    - CachePolicyId `658327ea-f89d-4fab-a63d-7e88639e58f6` used in DefaultCacheBehavior
    - `ViewerProtocolPolicy: redirect-to-https` on both distributions
    - `DefaultRootObject: index.html` on both distributions
  </acceptance_criteria>
  <done>CloudFormation template defines all 13 AWS resources (2 S3 buckets, 2 CloudFront distributions, 1 OAC, 1 CF Function, 1 ACM cert, 2 Route 53 records, 2 bucket policies, 1 OIDC provider, 1 IAM role) with correct dependencies and outputs</done>
</task>

</tasks>

<verification>
1. `infra/` directory contains `template.yaml` and `cf-function.js`
2. CloudFormation template has no circular dependencies (BucketPolicy references Distribution, not inline)
3. All 7 INFRA requirements addressed: INFRA-01 (S3 buckets), INFRA-02 (CloudFront + OAC), INFRA-03 (ACM cert), INFRA-04 (Route 53), INFRA-05 (CF Function)
4. CloudFront Function handles both trailing-slash and no-extension URI patterns
</verification>

<success_criteria>
- `infra/template.yaml` is valid CloudFormation YAML with all resources defined
- `infra/cf-function.js` contains SPA routing function
- Template uses parameters for environment-specific values (no hardcoded account IDs)
- OAC is used (not legacy OAI)
- S3 buckets are NOT configured as website endpoints
- Bucket policies are separate resources (avoiding circular dependency)
- ACM certificate uses DNS validation with Route 53 HostedZoneId
</success_criteria>

<output>
After completion, create `.planning/phases/03-infrastructure-and-deployment/03-01-SUMMARY.md`
</output>
